# vantage-frameworks v1.0.4 — Zod enum + readable validation errors

Fixes 2/5 tools previously unusable for LLM clients. Zod validation errors now return readable `isError` MCP responses. Tool schemas document enum values explicitly.

See [v1.0.0 release notes](https://github.com/elpiarthera/vantage-frameworks-mcp/releases/tag/v1.0.0) for full feature list.

## Install

```bash
npx -y @vantageos/mcp-frameworks
```
